

<%= form_with url: session_auth_path, method: :get do |form| %>

  <%= form.label :entered_pswd, "Enter Password" %>
  <%= form.password_field :entered_pswd %>

  <%= button_tag 'Login', name: :email, value: @email, class: "button" %>

<%end %>