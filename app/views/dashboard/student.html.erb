<h1>Dashboard#student</h1>
<p>Find me in app/views/dashboard/student.html.erb</p>
This is the student dashboard

<br>


<% if Current.user %>
  <table>
  <tr>
    <th> Survey </th>
    <th> Team </th>
    <th> Due Date </th>
    <th> Status </th>
  </tr>

    <% Task.where(:team_id => Current.user.team_id).each do |t| %>
      <% unless Rating.where(:rating_student => Current.user.id ).size >= 1 && Rating.where(:eval_id => t.eval_id).size >= 1 %>
        <tr>
      <td> <%=  link_to t.name, ratings_new_path(:eval_id => t.id), class: "survey" %> </td>
      <td> <%=  t.team_id  %> </td>
      <td> <%=  t.due_date  %> </td>
      <td> <%= t.status %> </td>
        </tr>
      <% end %>

    <%  end %>
  </table>
<% else %>
  <h1> Please log in to view your assigned evaluations. </h1>
  <%= link_to 'Login', session_login_path, method: :get %>
<% end %>
<%#TODO: Check for student rating id in ratings table %>
